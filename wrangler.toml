# Cloudflare Pages configuration
# For Cloudflare Pages deployment, the deploy command should be: wrangler pages deploy dist
# If using Git integration in Pages dashboard, no deploy command is needed (Pages handles it automatically)

name = "genar-me"
compatibility_date = "2025-11-03"

# Note: For Cloudflare Pages, redirects and headers should be in:
# - public/_redirects (will be copied to dist/_redirects)
# - public/_headers (will be copied to dist/_headers)
# These files are handled automatically by Cloudflare Pages.