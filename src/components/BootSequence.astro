---
// Boot Sequence Component
// Boot sequence overlay component
---

<div id="boot-overlay" class="boot-overlay">
  <div class="boot-output"></div>
</div>

<script>
  import { BootSequence } from '../terminal/boot/BootSequence';
  import { defaultBootConfig } from '../terminal/config/BootConfig';
  import { TimeoutManager } from '../terminal/utils/TimeoutManager';

  document.addEventListener('DOMContentLoaded', async () => {
    // Boot sequence is now handled by TerminalManager
    // This component serves as a placeholder for any boot-specific UI
    // and can be extended for custom boot overlays
    console.log('BootSequence component loaded');
  });
</script>

<style>
  .boot-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.95);
    z-index: 9999;
    display: none;
    align-items: center;
    justify-content: center;
    font-family: monospace;
  }

  .boot-overlay.active {
    display: flex;
  }

  .boot-output {
    color: #00ff00;
    font-size: 16px;
    text-align: left;
    white-space: pre-wrap;
    max-width: 80%;
    max-height: 80%;
    overflow-y: auto;
  }

  @media (max-width: 768px) {
    .boot-output {
      font-size: 14px;
      max-width: 90%;
    }
  }
</style>