---
// Screen Reflections Component
// Always-on CRT screen glass reflection overlay - independent of terminal power state
---

<div class="screen-reflections">
  <!-- Glass reflection effects - always visible -->
</div>

<style>
  .screen-reflections {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 9995;
    clip-path: url(#crtPath);
    border-radius: inherit;
    overflow: hidden;
    
    /* Enhanced glass reflection overlay with improved intensity */
    background:
      radial-gradient(
        ellipse 800px 600px at 20% 15%,
        rgba(255, 255, 255, 0.2) 0%,
        rgba(255, 255, 255, 0.12) 18%,
        transparent 38%
      ),
      radial-gradient(
        ellipse 400px 300px at 80% 80%,
        rgba(255, 255, 255, 0.12) 0%,
        rgba(255, 255, 255, 0.06) 28%,
        transparent 48%
      ),
      linear-gradient(
        180deg,
        rgba(255, 255, 255, 0.08) 0%,
        transparent 45%,
        rgba(0, 0, 0, 0.15) 100%
      );
    opacity: 0.4;
  }

  /* Mobile optimizations */
  @media (max-width: 768px) {
    .screen-reflections {
      opacity: 0.6;
    }
  }

  @media (max-width: 480px) {
    .screen-reflections {
      opacity: 0.5;
    }
  }
</style>